import { Routes, Route } from 'react-router-dom';
import Layout from '@layout/index';

// pages

const Router = () => {
  return (
    <Layout>
      <Routes></Routes>
    </Layout>
  );
};

export default Router;
